#include "EndpointFactory.hpp"

namespace ts
{

std::shared_ptr<EndpointNew> EndpointFactory::createEndpoint(const EndpointDescription& ed) const
{
    // try
    // {
    //     auto ep = std::make_shared<EndpointNew>
    // }
    return nullptr;
}

}